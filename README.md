# Generate prefix transacation hashes

Allow you to generate prefixed transaction hashes.

## Install

Setup project and install dependencies.

```sh
uv venv && source .venv/bin/activate
uv sync
```

Run:

```sh
uv run main.py
```
